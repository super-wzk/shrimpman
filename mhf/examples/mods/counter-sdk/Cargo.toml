[package]
name = "example-counter-sdk"
version = "1.0.0"
edition = "2024"

[features]
headers = ["safer-ffi/headers", "mhf-mod-sdk/headers"]

[dependencies]
safer-ffi = { version = "0.1.13", default-features = false, features = ["dyn-traits", "headers"] }
mhf-mod-sdk = { path = "../../../crates/runtime/mod-sdk" }
