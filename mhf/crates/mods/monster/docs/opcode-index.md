# 怪物 AI opcode 索引

`case 块`、`handler`、`操作`、`域` 取自本目录的逐区间记录，`摘要` 与
[`opcode-matrix.md`](opcode-matrix.md) 的语义矩阵一致；`0x00..0xff` 每个字节
恰好一行。`case 块` 是跳转表里的解释器 case 地址，`handler` 是它调用的原生
例程；`操作`、`域` 两列保留记录里的原始枚举值，含义见
[`opcode-catalog.md`](opcode-catalog.md)。

| opcode | case 块 | handler | 操作 | 域 | 摘要 |
| ---: | --- | --- | --- | --- | --- |
| `0x00` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x01` | `0x10869768` | `0x10860DC0` | confirmed | named | 用 actor 的 lane 掩码对照配置的 lane 数量做门控，不通过则扫到下一个 `0x01` 标记；选择子 1/2 是识别标记的续行形式 |
| `0x02` | `0x10869774` | `0x10860E70` | confirmed | named | 选择子 0 由 `+2687`（仇恨标志）门控，通过时清掉选中 lane 的字节并扫到 `0x02` 标记 |
| `0x03` | `0x10869780` | `0x10860EF0` | confirmed | named | 选择子 0 比较当前与待定的 16 位目标 id，目标不被接受时重置动作选择字段，再扫到 `0x03` 标记 |
| `0x04` | `0x1086978c` | `0x10860340` | confirmed | named | 把 `+2576`（主表下标）清 0，再装入 `main[0][0]`；不清 lane 掩码/delay/触发锁存 |
| `0x05` | `0x1086a089` | `0x10850ca0` | confirmed | named | 读三字节动作元组，必要时调用动作分派器，并按续行标志保存续行游标 |
| `0x06` | `0x1086979b` | `0x10866FB0` | confirmed | named | 设置脚本选择模式及相应的组/下标字段；模式含字面对、归一化 16 位目标 id、当前 lane 目标、合格 actor 选择 |
| `0x07` | `0x108697ae` | `(inline)` | confirmed | named | 写 `+2576 = 下标`、清 `+2580`，把当前游标切到 `main[0][下标]`（状态表项） |
| `0x08` | `0x108697ee` | `0x10860FD0` | confirmed | named | 选择子 0 在 `+1040`（实体状态字节）为 0 时直接返回，否则扫到 `0x08` 标记 |
| `0x09` | `0x10869808` | `0x108610D0` | confirmed | named | 选择子 0 在 `+1040` 等于 2 时直接返回，否则扫到 `0x09` 标记 |
| `0x0a` | `0x10869815` | `(inline)` | confirmed | named | 把载荷字节写入 `+2088` |
| `0x0b` | `0x10869826` | `0x10861160` | confirmed | named | 选择子 0 把 `+2680`（mind 状态 a）拷进 `+2600` 并与载荷字节比较，不等则扫描 |
| `0x0c` | `0x10869833` | `0x108611F0` | confirmed | named | 按第一个载荷字节选字段族做置位/清位，部分分支还受 actor 类型字节 `+3` 与 `+2739`（命令模式标志）门控 |
| `0x0d` | `0x10869842` | `0x108612F0` | confirmed | named | 清掉 `0x0C` 处理的同一批字段族；最后一个分支还把 `+2846`（保存的动作 id）置 -1 |
| `0x0e` | `0x10869851` | `0x10861400` | confirmed | named | 选择子 0 比较当前目标上下文与物种/actor 上下文，不满足则扫到 `0x0E` 标记；另有归一化目标 id 的快路径 |
| `0x0f` | `0x10869860` | `0x10861530` | confirmed | named | 存四个上下文字节，标记续行上下文，并在表可用时按载荷第 3 字节选脚本表项 |
| `0x10` | `0x1086986d` | `0x10861660` | confirmed | named | 递增脚本选择计数，按上下文模式 0/1/2 选下一个表项；模式 0 用 `+68` 作确定性下标 |
| `0x11` | `0x1086987e` | `0x108617C0` | confirmed | named | 扫 `+2821` 的置位，存最高 lane 下标，并设当前动作模式 1/组 0 |
| `0x12` | `0x10869891` | `0x10861830` | confirmed | named | 扫 `+2687`（仇恨标志），把最高 lane 同时存为当前下标与 `+2612`（cmd_pl_target），并清两个续行标志 |
| `0x13` | `0x108698a4` | `0x108618B0` | confirmed | named | 设动作模式 1/组 0，把 `+2612`（cmd_pl_target）当当前下标（非 -1 时掩到 4 位） |
| `0x14` | `0x108698c6` | `0x108618F0` | confirmed | named | 把载荷字节换算成角度阈值，算到所选目标上下文的相对角，超阈值则扫到 `0x14` 体标记 |
| `0x15` | `0x108698d7` | `0x10861BB0` | confirmed | named | 选择子 0 读计数与大端目标 id 列表，逐个归一化，未命中则跳过嵌套 `0x15` 体 |
| `0x16` | `0x108698e6` | `0x10861CF0` | confirmed | named | 存脚本表选择子，把 `+2608`（第三保存游标）指向载荷续行，并把对应表项装进 `+2652`（分派游标） |
| `0x17` | `0x108698f5` | `0x10861D30` | confirmed | named | 只在 `+2844`（区域移动计数）未置位时初始化四字节目标上下文：目标模式、计数、表选择子、上下文标志 |
| `0x18` | `0x10869904` | `0x10862010` | confirmed | named | 目标上下文模式大于 1 且全局门允许时，标记上下文有效、调用目标选择、装入选中的脚本指针 |
| `0x19` | `0x1086990f` | `(inline)` | confirmed | named | 把 `+3208`（maji_next_stage_no）拷进当前动作元组（模式 3/组/下标），并调用该路径使用的原生空钩子 |
| `0x1a` | `0x10869941` | `0x108620D0` | confirmed | named | 读大端 16 位目标 id，归一化后写入待定/当前目标与动作选择字段 |
| `0x1b` | `0x1086994e` | `0x10862120` | confirmed | named | 选择子 0 比较载荷字节与 `+2681`（mind 状态 b），否则扫到 `0x1B` 标记 |
| `0x1c` | `0x1086995b` | `0x10862210` | confirmed | named | 选择子 0 用两个原生哈希式函数算确定性比值，与阈值列表比较，再走嵌套 `0x1C` 体 |
| `0x1d` | `0x1086996a` | `0x108622D0` | confirmed | named | 选择子 0 拿 `+2682` 与有序字节列表比较，跳过嵌套 `0x1D` 体直到第一个匹配或更大的项 |
| `0x1e` | `0x10869977` | `(inline)` | confirmed | named | 清当前选择 id、游标、目标下标及相关上下文字节 |
| `0x1f` | `0x108697fb` | `0x10861050` | confirmed | named | 选择子 0 仅在 `+1040` 不等于 1 时继续，否则扫到 `0x1F` 标记 |
| `0x20` | `0x1086999e` | `0x108623A0` | confirmed | named | 选择子 0 取目标位置，算相对 `+164`（朝向）的归一化相对角，与阈值表比较后走嵌套 `0x20` 体 |
| `0x21` | `0x108699ad` | `0x10862510` | confirmed | generic: actor+2696, actor+2708 | 选择子 0 仅在 `+2696` 大于 `+2708` 乘全局系数时继续，否则扫标记 |
| `0x22` | `0x108699ba` | `0x108625B0` | confirmed | named | 选择子 0 把 actor 位置与保存参考点 `+2852`/`+2856`/`+2860` 的距离与缩放后的阈值字节比较，超距离则跳过嵌套体 |
| `0x23` | `0x108699d6` | `0x108627C0` | confirmed | named | 选择子 0 用最多 40 条 actor 记录比类型列表（含 `0xB1` 特例），无匹配则走嵌套 `0x23` 体 |
| `0x24` | `0x108699e5` | `0x108628F0` | confirmed | named | 选择子 0 存重复次数与体游标；选择子 1 递减计数，仍为正则回到体 |
| `0x25` | `0x108699f2` | `(inline)` | confirmed | named | 清 `+2623`（解释器停止标志 2），也就是 `0x24` 用的重复计数器 |
| `0x26` | `0x108699fe` | `(inline)` | confirmed | named | 把载荷字节写入 `+2738` |
| `0x27` | `0x10869a0f` | `0x10862960` | confirmed | generic: +27 | 拿 `+27`（关联 actor 指针）与有序字节列表比较，按结果跳过嵌套 `0x27` 体 |
| `0x28` | `0x10869a1c` | `0x10862E40` | confirmed | named | 扫活动 actor 记录，找与 `+2040`（动作 id）同归一化物种的记录；没找到则走嵌套 `0x28` 体 |
| `0x29` | `0x10869a29` | `0x10862F40` | confirmed | named | 仅在带符号的 `+2910` 为正时继续，否则走嵌套 `0x29` 体 |
| `0x2a` | `0x10869a36` | `0x10862FC0` | confirmed | generic: +2912 | 仅在带符号的 `+2912` 为正时继续，否则走嵌套 `0x2A` 体 |
| `0x2b` | `0x10869a43` | `0x10863040` | confirmed | named | 选择子 0 比较九个运行字段/actor 类型组合之一与载荷字节，不等则跳过嵌套 `0x2B` 体 |
| `0x2c` | `0x10869a50` | `0x10863180` | confirmed | named | 用物种字节 `+3` 与每个载荷字节索引按物种键表 `0x11A4E9C0`，比较表项首字节；无匹配则走嵌套体 |
| `0x2d` | `0x10869a6a` | `(inline)` | confirmed | named | 设动作模式 7，把 `+2922`（两态字节 a）拷进当前动作组/下标字段 |
| `0x2e` | `0x10869a93` | `0x1084FB80` | confirmed | named | 由 actor 类型与载荷字节重算 `+1968`；特殊类型选不同的上下文表偏移与模式值 |
| `0x2f` | `0x10869aa9` | `0x108632F0` | confirmed | named | 在配置的 lane 里找低位可用标志非零的项；没找到则走嵌套 `0x2F` 体 |
| `0x30` | `0x10869ab8` | `0x108633B0` | confirmed | named | 找到第一个低位可用标志非零的 lane，把 `+3190` 置 1 |
| `0x31` | `0x10869ac9` | `(inline)` | confirmed | named | 把当前动作模式字节设为 8 |
| `0x32` | `0x10869ad6` | `0x10863400` | confirmed | named | 选择子 0 比较 `+2088` 与载荷字节，不等则走嵌套 `0x32` 体 |
| `0x33` | `0x10869ae3` | `0x10863480` | confirmed | named | 选择子 0 拿 `+2088` 与载荷列表比较，直到命中前走嵌套 `0x33` 体 |
| `0x34` | `0x10869af0` | `0x10863510` | confirmed | named | 选择子 0 比较 actor 的 `+20`/`+21` 字节与两个载荷字节，任一不同则走嵌套 `0x34` 体 |
| `0x35` | `0x10869afd` | `0x108635A0` | confirmed | named | 选择子 0 仅在 `+2726` 为 0 时继续，否则走嵌套 `0x35` 体 |
| `0x36` | `0x108699c9` | `0x10862700` | confirmed | named | 选择子 0 算 actor 位置（`+172`/`+180`）与保存参考点（`+2852`/`+2860`）的距离，超阈值则走嵌套 `0x36` 体 |
| `0x37` | `0x10869b0a` | `0x10863620` | confirmed | named | 选择子 0 仅在全局对象标志 `+44` 不含位 `0x200000` 时继续，否则走嵌套 `0x37` 体 |
| `0x38` | `0x10869b17` | `0x108636B0` | confirmed | generic: 0x1186916C | 选择子 0 按物种 id `+2040` 与变体 `+2008` 读表项，表项非零则抑制嵌套 `0x38` 体 |
| `0x39` | `0x10869b24` | `0x10863750` | confirmed | named | 选择子 0 仅在 `+2914` 为 0 时继续，否则走嵌套 `0x39` 体 |
| `0x3a` | `0x10869b31` | `0x108637D0` | confirmed | named | 选择子 0 仅在 `+3186` 非零时继续，否则走嵌套 `0x3A` 体 |
| `0x3b` | `0x10869b3e` | `0x10863850` | confirmed | named | 选择子 0 仅在 `+3172`（关联 actor 指针）为空时继续，否则走嵌套 `0x3B` 体 |
| `0x3c` | `0x10869b4b` | `0x108638D0` | confirmed | named | 选择子 0 只在 `+3172` 指向的 actor 其 `+2680` 等于 1 且物种 id 相同时抑制嵌套体 |
| `0x3d` | `0x10869b58` | `0x10863970` | confirmed | named | 选择子 0 比较经 `0x10B4C5E0` 归一化的大端 id 与 `+2846`（保存的动作 id），不等则走嵌套 `0x3D` 体 |
| `0x3e` | `0x10869b67` | `0x10863A40` | confirmed | named | 选择子 0 读计数与大端 id 列表，逐个归一化，直到命中前走嵌套 `0x3E` 体 |
| `0x3f` | `0x10869b76` | `0x10863B20` | confirmed | named | 读大端目标 id 与半径字节，解析目标表，选匹配或最近子项，装入脚本上下文与游标 |
| `0x40` | `0x10869b89` | `0x108693e0` | confirmed | named | 读一字节，为 0 时以 0、否则以 1 调用 `0x108693E0` |
| `0x41` | `0x10869bb0` | `0x10863f40` | confirmed | generic: 0x118e45b0, 0x119b63a8 | actor 类型 `+3` 为 27（或 28/31）且选择子为 0 时，要求三个谓词返回 0，算出缩放角度并调用 `0x10858E00` |
| `0x42` | `0x10869bc3` | `0x10864010` | confirmed | generic: 0x116038a8, 0x119b5f9c, 0x119b5fa0 | 选择子 0 刷新向量，由后一字节得阈值，与 `0x10855830` 的绝对角差比较，按结果跨过 `0x42` 标记块 |
| `0x43` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x44` | `0x10869be1` | `0x108642d0` | confirmed | named | 选择子 0 仅在 `+3172` 为空或其 `+2040` 与当前 `+2040` 不同时进入体；选择子 1 扫到终止符 |
| `0x45` | `0x10869bee` | `0x10864370` | confirmed | generic: 0x11406af0 | 选择子 0 扫四条全局记录（玩家域，`0x1DC6B750`，4176 步长），按活动掩码、当前 `+2040`、actor 类型 `+3`、记录标志过滤，按 `+2740` 浮点 lane 值排名，把选中下标写入 `+2612`（cmd_pl_target） |
| `0x46` | `0x10869bfd` | `0x10864500` | confirmed | named | 选择子 0 读两字节对，与选中 lane 记录的 `+20`/`+21` 比较，不等则跨过 `0x46` 标记块 |
| `0x47` | `0x10869c0a` | `0x108645b0` | confirmed | generic: +2729 | 选择子 0 清 `+2729`，非零时立即返回，否则扫 `0x47` 标记块 |
| `0x48` | `0x10869c17` | `(inline)` | confirmed | named | 把载荷字节（0 视作 0）写入 `+3228`（延迟 tick） |
| `0x49` | `0x10869c3e` | `0x10864640` | confirmed | named | 用 `+2612`（cmd_pl_target）填当前动作字段；无选中 lane 时设类型 `+2581`=1、下标 `+2582`=0、值 `+2584`=-1，否则类型 11、下标 = lane 与 0xf |
| `0x4a` | `0x10869c4d` | `0x108646b0` | confirmed | named | 选择子 0 在选中 lane 位出现在 `+2684`（模式重置标志字节）且 lane 不为 `0xff` 时直接返回，否则扫 `0x4a` 标记块 |
| `0x4b` | `0x10869c5a` | `(inline)` | confirmed | named | 有选中 lane 时清 `+2788`（仇恨表 1）+ 4×(`+2612` & `0xf`) 处的 dword |
| `0x4c` | `0x10869c7b` | `(inline)` | confirmed | named | 有选中 lane 时清 `+2824`（仇恨表 2）+ 4×(`+2612` & `0xf`) 处的 dword |
| `0x4d` | `0x10869c9c` | `0x108538b0` | confirmed | named | 以该对象调用 `0x108538B0` 刷新向量 |
| `0x4e` | `0x10869cb1` | `0x10864750` | confirmed | generic: runtime +2708, runtime +2696 | 选择子 0 把 `+2708` 的一半加到 `+2696` 并夹到 `+2708` |
| `0x4f` | `0x10869cc0` | `0x10864930` | confirmed | generic: runtime +2712, runtime +2700 | 选择子 0 把 `+2712` 的一半加到 `+2700` 并夹到 `+2712`，再调 `0x10864790` |
| `0x50` | `0x10869ccf` | `0x10864970` | confirmed | generic: runtime +2716, runtime +2704 | 选择子 0 把 `+2716` 的一半加到 `+2704` 并夹到 `+2716` |
| `0x51` | `0x10869cde` | `0x108649b0` | confirmed | named | 选择子 0 在 `+1040` 等于 4 时跳过体，否则扫 `0x51` 标记块 |
| `0x52` | `0x10869ceb` | `0x10864a30` | confirmed | named | 玩家域候选 lane 搜索：按 `+2687`（仇恨标志）、`+1826`、`+2040`、`+2824` 与辅助谓词过滤，按 `+2740` 浮点值排名，把选中 lane 写入 `+2612` |
| `0x53` | `0x10869cfc` | `0x10864d90` | confirmed | generic: +3268 | 第二轮玩家域候选 lane 搜索：用活动掩码、匹配的 `+2040`/`+1252` 值、`+2824` 阈值、`+2740` 排名与专用辅助检查，写 `+2612`（cmd_pl_target） |
| `0x54` | `0x10869d0f` | `0x10865450` | confirmed | named | 选择子 0 在选中 lane 记录有效、其 `+2042` 为零且 `0x10A94CE0` 返回非零时直接返回，否则扫 `0x54` 标记块 |
| `0x55` | `0x10869d1e` | `0x10865510` | confirmed | named | 选择子 0 在无选中 lane 或该 lane 的 `+2824` 小于 30000 时扫 `0x55` 标记块 |
| `0x56` | `0x10869d2b` | `0x10865860` | confirmed | generic: 0x1e8001ec | 选择子 0 读大端 `u16`，与全局 `0x1E8001EC`+8 的字不等时扫 `0x56` 标记块 |
| `0x57` | `0x10869d38` | `0x10865910` | confirmed | generic: +3185 | 按选择子扫标记：选择子 0 读计数与大端 `u16` 值，与 `+3185` 比较后在 `0x57` 标记处前进或停下 |
| `0x58` | `0x10869d45` | `0x10865a10` | confirmed | named | 设动作类型 `+2581`=1、下标 `+2582`=0；若 `+3172`（em 数组里的关联 actor 指针）存在则把其 `+1826`（该 actor 自己的目标槽）拷进 `+2584` 与 `+2612`，否则都置 -1 |
| `0x59` | `0x10869d58` | `0x10865a60` | confirmed | generic: +3200 | 选择子 0 仅在 `+3200` 为零时扫 `0x59` 标记块 |
| `0x5a` | `0x10869d65` | `0x10865ae0` | confirmed | named | 选择子 0 在保存的动作类型 `+3231` 为 11 或 1 且选中 lane 时，若值等于 `0x1086A530`(lane`+2040`, lane`+2008`) 则立即返回 |
| `0x5b` | `0x10869d74` | `0x10865bc0` | confirmed | named | 选择子为 0 时，对 `+2684` 里没有对应 lane 位的项，清 `+2688`（仇恨值）+ 2×i 处的字 |
| `0x5c` | `0x10869d83` | `0x10865c20` | confirmed | named | 选择子 0 在 `+2040` 等于 `global+20` 且 `0x108CF5B0`(`+172`, …) 成功时立即返回，否则扫 `0x5c` 标记块 |
| `0x5d` | `0x10869d92` | `0x10865cf0` | confirmed | named | 选择子 0 刷新向量，`+2852`/`+2856`/`+2860` 三个浮点都非零则返回，否则调用 `nullsub_2` 并扫 `0x5d` 标记块 |
| `0x5e` | `0x10869da1` | `0x10865e10` | confirmed | generic: 0x11869168 | 选择子 0 拿载荷字节与 `0x1086A530`(`+2040`, `+2008`) 比较，并对表值 365/367/369 有特殊处理 |
| `0x5f` | `0x10869db0` | `0x10865f30` | confirmed | generic: +1005, 0x1188eab0 | 选择子驱动 lane 搜索：按 `+2687`（仇恨标志）、活动记录、匹配 `+2040` 与辅助分类过滤，按 `+2740` 或 `+2824` 排名后写 `+2612` |
| `0x60` | `0x10869dc3` | `0x108663b0` | confirmed | generic: 0x1e7fff3c | 选择子 0 仅在全局字节 `0x1E7FFF3C`+9466 为 0 时扫 `0x60` 标记块 |
| `0x61` | `0x10869dd0` | `0x1085c170` | confirmed | generic: +2912, 0x118c7308 | 用全局表 `0x118C7308` 中由 `+3` 选出的指针调用 `0x1085C170`；反编译里有一处从临时量写 `+2912` 的痕迹，只作为未验证记录保留 |
| `0x62` | `0x10869df4` | `0x10866440` | confirmed | named | 选择子 0 跳过两字节载荷，按辅助函数得出的选择子反复扫 `0x62`/`0x98` 标记；选择子 1..3 扫到 `0x62` 终止符 |
| `0x63` | `0x10869e05` | `0x10866570` | confirmed | generic: +27 | 选择子 0 读掩码字节，(掩码 & `+27`) 为 0 时扫 `0x63` 标记块 |
| `0x64` | `0x10869e12` | `0x10866c60` | confirmed | named | 选择子 0 读索引字节，无选中 lane 或 lane 计时器 `+2824` 低于全局阈值表 `0x11A4BFB0[index]` 时扫 `0x64` 标记块 |
| `0x65` | `0x10869e1f` | `(inline)` | confirmed | named | 清 `+2824`（仇恨表 2）起连续四个 dword：`+2824`/`+2828`/`+2832`/`+2836` |
| `0x66` | `0x10869e3f` | `0x10866d00` | confirmed | named | 选择子 0 仅在全局记录集全部为空时扫 `0x66` 标记块 |
| `0x67` | `0x10869e50` | `0x10866dd0` | confirmed | named | 选择子 0 在 `+2739`（命令模式标志）为 0 时立即返回，否则扫 `0x67` 标记块 |
| `0x68` | `0x10869e5d` | `0x10860430` | confirmed | named | 当 `+2739`（命令模式标志）与 `+2659` 都为 0 时，置 `+2659`=2、`+2622`=1 并调用 `0x10860430` |
| `0x69` | `0x10869bd2` | `0x10864170` | confirmed | generic: 0x116038a8, 0x119b5f9c, 0x119b5fa0 | 选择子 0 刷新向量，由后一字节缩放得阈值，与绝对角差比较后走 `0x69` 标记分支；另要求阈值小于 `0x4000` |
| `0x6a` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x6b` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x6c` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x6d` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x6e` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x6f` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x70` | `0x10869a5d` | `0x10863220` | confirmed | named | 选择子 0 读计数，把列表字节与 `+3`（物种 id）比较，扫过或跳过嵌套 `0x70` 块；选择子 1/2 扫到选择子 3 |
| `0x71` | `0x10869f4b` | `0x10868380` | confirmed | named | 选择子 0 把载荷字节与带符号 `+3212`（带符号阈值）比较，不满足则扫嵌套 `0x71` 块 |
| `0x72` | `0x10869f58` | `0x10868410` | confirmed | generic: 0x1e8001ec, 0x1ed52870, 0x1ed52951 | 选择子 0 由全局 `0x1E8001EC`+8、`0x1ED52870`、`0x1ED52951` 门控后扫嵌套 `0x72` 块 |
| `0x73` | `0x10869f65` | `0x108684b0` | confirmed | named | 选择子 0 把列表与 `+2920`（目标标志字）按 `0x7f` 掩码后比较，相等时清掉保留高位的字段 |
| `0x74` | `0x10869f72` | `0x10868590` | confirmed | generic: 0x1e8001ec, 0x1ed52870, 0x1ed52951 | 选择子 0 把载荷字节与 `+2920`（目标标志字）按 `0x7f` 掩码后比较；受保护的辅助函数可让条件直接成立 |
| `0x75` | `0x10869f81` | `0x10862a30` | confirmed | generic: +2930 | 选择子 0 把列表字节与 `+2930`（`0x75` 比较字节）比较，跳过或扫过嵌套 `0x75` 块 |
| `0x76` | `0x10869f8e` | `0x10862b00` | confirmed | generic: 0x1e8001ec | 选择子 0 由全局 `0x1E8001EC`+2357 的位推出比较字节，比较列表后扫嵌套 `0x76` 块 |
| `0x77` | `0x10869f9b` | `0x108665f0` | confirmed | generic: 0x1ed6bca0 | 选择子 0 仅在全局字节 `0x1ED6BCA0` 置位 `0x10` 且清位 `0x08` 时继续 |
| `0x78` | `0x10869fa8` | `0x10866ae0` | confirmed | named | 选择子 0 读上下界字节，由 `+2068`（角度来源）与 `+164` 算当前角，条件成立才扫嵌套 `0x78` 块 |
| `0x79` | `0x10869fb7` | `0x10862d20` | confirmed | named | 选择子 0 用载荷字节 2 调 `+1140`（行为对象指针）+12 的函数指针，把返回字节与从偏移 4 开始的有序表比较 |
| `0x7a` | `0x10869fc6` | `0x10862c00` | confirmed | generic: 0x118648f0, 0x1e7fff3c | 选择子 0 把全局 `0x1E7FFF3C`+52 经表 `0x118648F0` 映射后比较列表，再扫嵌套 `0x7a` 块 |
| `0x7b` | `0x10869ef3` | `(inline)` | confirmed | named | 递增 `+1096`（脚本 RNG 计数），只消耗 opcode 字节 |
| `0x7c` | `0x10869fd3` | `0x10866680` | confirmed | generic: 0x118648f0, 0x1e7fff3c | 选择子 0 把全局 `0x1E7FFF3C`+52 经 `0x118648F0` 映射，映射值为 9/10/53/54 时直接继续，否则扫嵌套 `0x7c` 块 |
| `0x7d` | `0x10869fef` | `0x10868680` | confirmed | named | 选择子 0 读计数与候选字节，用 `0x1087CB30`(`+3`, 候选) 逐个测试，未命中前跳过嵌套 `0x7d` 块 |
| `0x7e` | `0x108698b3` | `0x108655b0` | confirmed | generic: 0x1188eab0, 0x1ed7ad2c, 0x1edb768e | 清 `+2582` 与 `+2612`；先按玩家域 lane 搜索 `sub_10864D90`，未命中再切到 em 域：在 `dword_1ED7AD2C`（3824 步长，最多 40 条）里挑"非自身、活动、`+3187`≠0、`+2040` 与自身相同、且在半径/高度内"的记录，把该记录自己的下标 `+12` 写进 `+2612`/`+2584`，命令 kind 置 13 |
| `0x7f` | `0x10869fe0` | `0x10866770` | confirmed | named | 选择子 0 仅在 `0x1086A920` 返回 0 时清 `+1042`（实体状态字）的位 `0x40`，然后扫嵌套 `0x7f` 块 |
| `0x80` | `0x10869e88` | `0x10866e50` | confirmed | named | 选择子 0 读计数，扫嵌套 `0x80` 记录，把每条第 3 字节的带符号增量按 `+1096` 与 31 偏移累加 |
| `0x81` | `0x10869e95` | `0x10866f20` | confirmed | named | 按索引字节装入主表游标，把下标存 `+2577`（保存的主下标），stage `+2580` 为 0 时把续行游标记到 `+2552`，再置 stage=1 |
| `0x82` | `0x10869ebb` | `0x10866f60` | confirmed | named | 由两级索引装入二级游标，把下标存 `+2578`，stage `+2580` 为 1 时把续行游标记到 `+2556`，再置 stage=2 |
| `0x83` | `0x10869ee6` | `0x10867460` | confirmed | generic: +1112 | 选择子 0 把计数列表与选中 lane 值比较（`+3231` 为 13 时改用距离推导值），把值写 `+1112`，并把跳过计数存 `+2586` |
| `0x84` | `0x10869ef3` | `(inline)` | confirmed | named | 与 `0x7b` 共用内联递增路径：递增 `+1096`（脚本 RNG 计数） |
| `0x85` | `0x10869ffe` | `0x113aaac0` | confirmed | named | 置 `+1042`（实体状态字）位 `0x40` 并调用 `0x113AAAC0` |
| `0x86` | `0x1086a011` | `(inline)` | confirmed | named | 清 `+1042`（实体状态字）位 `0x40` |
| `0x87` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x88` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x89` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x8a` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x8b` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x8c` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x8d` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x8e` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x8f` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x90` | `0x10869f0f` | `0x10868210` | confirmed | named | 用载荷字节索引 `0x118C5784[10×(`+3`)]`，把三个浮点拷进 `+172`/`+176`/`+180` 与 `+1784`/`+1788`/`+1792` |
| `0x91` | `0x10869f1e` | `0x10868270` | confirmed | named | 由 `+172` 起的向量算出无符号 16 位结果存进 `+164`（朝向），并消耗载荷字节 |
| `0x92` | `0x10869efa` | `(inline)` | confirmed | named | 除消耗 opcode 字节外没有观察到其他动作 |
| `0x93` | `0x10869efa` | `(inline)` | confirmed | named | 与 `0x92` 相同的空实现 |
| `0x94` | `0x10869f3e` | `0x108682b0` | confirmed | named | 选择子 0 把计数列表与全局 dword `0x11C6A7E8` 比较并扫嵌套 `0x94` 块 |
| `0x95` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x96` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x97` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x98` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x99` | `0x10869f29` | `(inline)` | confirmed | named | 把载荷字节左移 8 位写入 dword `+164`（朝向），共消耗 2 字节 |
| `0x9a` | `0x1086a023` | `0x10866820` | confirmed | generic: runtime +2712, runtime +2700 | 选择子 0 把载荷字节按全局浮点 `0x119B5EF0` 与 `+2712` 缩放，与 `+2700` 比较后条件性跳过嵌套 `0x9a` 块 |
| `0x9b` | `0x1086a030` | `0x108668e0` | confirmed | named | 选择子 0 用原始记录标志、辅助函数与记录偏移 `+1040` 检查 `+1826`（玩家域下标，解析到 `0x1DC6B750 + 4176*slot`）处的选中记录，全部通过则立即返回 |
| `0x9c` | `0x1086a041` | `0x10866a00` | confirmed | named | 选择子 0 把载荷字节放大 100 倍，仅当动作字段 `+2581`/`+2582`/`+2584` 与 `+2856`/`+1800` 距离满足观察到的谓词时才穿过嵌套体 |
| `0x9d` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x9e` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0x9f` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa0` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa1` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa2` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa3` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa4` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa5` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa6` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa7` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa8` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xa9` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xaa` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xab` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xac` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xad` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xae` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xaf` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb0` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb1` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb2` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb3` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb4` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb5` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb6` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb7` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb8` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xb9` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xba` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xbb` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xbc` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xbd` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xbe` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xbf` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc0` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc1` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc2` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc3` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc4` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc5` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc6` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc7` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc8` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xc9` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xca` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xcb` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xcc` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xcd` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xce` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xcf` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd0` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd1` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd2` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd3` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd4` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd5` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd6` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd7` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd8` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xd9` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xda` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xdb` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xdc` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xdd` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xde` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xdf` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe0` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe1` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe2` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe3` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe4` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe5` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe6` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe7` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe8` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xe9` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xea` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xeb` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xec` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xed` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xee` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xef` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf0` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf1` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf2` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf3` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf4` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf5` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf6` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf7` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf8` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xf9` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xfa` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xfb` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xfc` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xfd` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xfe` | `0x1086a0ba` | `default` | default_stop | n/a | 解释器跳转表没有这个字节的项，所以走 `0x1086a0ba` 的 default 路径：当命令模式 `+2739` 与脚本状态 `+2659` 都为零时，把脚本标记为停机（`+2659 = 2`），然后总是设置停止标志 `+2622 = 1`，运行 `0x10860430`（清 lane 掩码 `+3288`、延迟 `+3228`、触发锁存 `+2601`，重置主表下标/stage，游标 = `main[0]`，清 route 计数器 `+2840/+2841/+2844/+2845`，以及 `+2846/+3208`，除非物种是 `0x8e`），并离开分派循环。 |
| `0xff` | `0x10869f00` | `0x108675a0` | confirmed | named | 控制前缀：后一字节是选择子；`0x00..0x06` 与 `0xf5..0xff` 是命令，其余选择子交回解释器按普通 opcode 执行（此时 `0xff` 只消耗自身） |
