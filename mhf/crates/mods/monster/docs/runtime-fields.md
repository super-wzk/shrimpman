# 怪物 AI 运行时字段

本文件记录固定 `mhfo-hd.dll`（镜像基址 `0x10000000`，SHA-256
`95c580195f4080d2e9582c8c9df36abeb280476e088b6366583c5f138da8f301`）里
解释器的 `this`/`a2`/`esi` 指针实际指向什么。它是逐 opcode 语义的支撑证据：
只有原生消费者证明了角色，操作数才会被赋予角色；只有发行版自己打印或存过的
名字，才会被当作游戏词汇使用。

## 名字是怎么恢复的

解释器对象是怪物（"em"）实体。下面的名字来自四类原生证据：

- 发行版仍传给空 logger `nullsub_2` 的调试标签字符串：
  `"T_KIND_AREA maji_next_stage_no:%d"`（`0x10869917`）、
  `"FIND_NG_END em->cmd_pl_target == NOT_FOUND_PL"`（`0x10867D41`）、
  `"KEHAI_END route_ptr_set( em, em->cmd_route_act )"`（`0x10867EFC`）、
  `"KEHAI_END cmd = em_cmd_top( em ) em_g_init_flag_set(em,EM_NET_CMD)"`
  （`0x10867F20`）、`"FIND_END Em_Mode_Chg( em, EM_MODE_ATTACK )"`（`0x10867EA1`）
  和 `"em_cmd_mind_ck MIND CKH:%d(%s) != NOW:%d"`（`0x1086218B`）；
- 这些打印使用的名字表：actor 类型名 `EM_EXC` 与 `KIND_EMY..KIND_NOT` 在
  `0x1199D870..0x1199D918`（指针数组在 `0x11A4BF74`，后面跟着 act-id 基数表
  `0x11A4BFB0`），命令名
  `EM_EXC/EM_VSD/EM_REV/EM_DEM/EM_DIE/EM_DMG/EM_ATK/EM_FLY/EM_MOV/EM_ACT`
  在 `0x1199D918..0x1199D960`；
- 物种名表 `EM_*` 在 `0x1199CE9C..`，结合 `0x10860360` 里的表选择
  `off_11A4BD68[map][*(em+3)]`，`map` 即 `dword_1E7FFF3C+0x34` 的任务 MapID；
- 游标 helper 实际走的表布局：`main[0]` 在 `0x10860340`、`main[8]` 在
  `0x108604C0`、`main[6]` 在 `0x10862010`（该函数还打印
  `"em_cmd_area_route_move"`，`0x1086208A` 起）。`main[N]` 一律指物种块的
  dword 下标，所以 `main[6]` 就是 `[eax+18h]`，不是字节偏移 24。

下面的偏移是十进制字节偏移，默认从实体指针起，除非明确写了别的指针。

## Actor 存储与全局

| 地址 | 含义 | 证据 |
| --- | --- | --- |
| `byte_1DC6B750` | 玩家记录数组，步长 4176（`0x1050`）；下标 `i` 是 `byte_1DC6B750 + 4176*i`。所有已提交命令 kind 不是 13 的目标解析都落到这里。 | `0x108538B0` case 1 写 `+1100 = &byte_1DC6B750[4176*slot]`（`0x10853978`）；`0x10855B30` 对除 13 以外的每个 kind 都选这个数组；`0x10864A30` 遍历同一个数组（`v6` 从 `word_1DC6BF48` = base + 2040 开始，`v6 += 2088` u16），并用同一下标索引逐项仇恨表；`0x9B`（`0x108668E0`）通过它解析 `+1826` |
| `dword_1ED7AD2C` | actor/em 记录数组，步长 3824（`0xEF0`），最多 40 项。包含解释器自己运行的记录，以及关联 actor 查找返回的记录。 | `0x108655B0` 跳过 `v9 == a1`；`0x108578A0` 把 `result[3]` 与本记录 `+3` 推导出的常量比较；记录带 `+3` actor id、`+12` 自己的下标、`+2040` act id 和 `+3187` |
| `dword_1E7FFF3C + 9210` | 解释器遍历 4176 步长（玩家）数组时用作循环上界的项数。 | `0x10864A30`、`0x108693E0` |
| `dword_1E7FFF3C + 20` | 当前全局 id（`u16`，`0xFFFF` = 无），tick 门在进入解释器前读取。 | `0x1085C5E0` |
| `dword_1E8001EC + 8` | 初始化时清 actor 位置之前测试的 `u16`。 | `0x1086EAD0` |
| `dword_1E8001EC + 128` | 命令请求被接受前校验的对象，其 `+46`（`u16`）会与 `21731`、范围 `0x54F1..0x54F8` 比较，并传给 `sub_101CFD00`。 | `0x10855470` |

上面的绝对地址是镜像自身的全局（段 `seg002`）；在文件镜像里，这些运行时
指针初始化为 `-1`。

## 解释器对象字段

### 游标与表状态

| 偏移 | 类型 | 含义 | 证据 |
| ---: | --- | --- | --- |
| `+2544` | `void***` | 主脚本表。`[0]` 主条目（状态表）；`[3]`/`[4]`/`[8]`/`[10]`/`[11]`/`[13]`/`[14]` 是七个事件槽，下标即 dword 序号（mask `0x10`/`0x20`/`0x02`/`0x04`/`0x08`/`0x80`/`0x40`）；`[6]` 是地面区域移动（`em_cmd_area_route_move`）脚本表。由 `0x10860360` 按物种/stage 选择，由 `0x10860340`、`0x108604C0`、`0x10862010` 和事件分派器 `0x10860430` 消费。 | `0x10860340`、`0x108604C0`、`0x10862010`、`0x1086052A`（`main[14]`）、`0x1086056B`（`main[13]`）、`0x108605AB`（`main[4]`）、`0x108605F3`（`main[3]`）、`0x1086063B`（`main[11]`）、`0x10860683`（`main[10]`）、`0x108606CB`（`main[8]`） |
| `+2548` | `u8*` | 基 lane 游标；`+3288` 没有 lane 位置位时使用的静止游标。 | `0x10860700`、`0x108614F0` |
| `+2552` | `u8*` | 主脚本续行游标；`0x81` 在 stage 0 保存，`FF 01` 返回此处。 | `0x1086760F`、`0x10866F20` |
| `+2556` | `u8*` | 第一层子脚本续行游标；`0x82` 在 stage 1 保存，`FF 02` 返回此处。 | `0x10867630`、`0x10866F60` |
| `+2576` | `u8` | 当前主表下标。打印为 `MAIN %d`，由 `0x07`、`0x04`、`FF 00/FC` 写入，由 `0x10860340` 读取。 | `0x108697C3`、`0x108697E3` |
| `+2577` | `u8` | `0x81` 调用的 root[1] 子脚本索引，不是主状态索引。 | `0x10866F20` |
| `+2580` | `u8` | 当前选择的 stage：`0` = 主脚本，`1` = root[1] 子脚本，`2` = root[15 + group] 二级子脚本。 | `0x81`、`FF 01`、`FF 02` |
| `+2581` | `u8` | 当前命令 kind，与 `+2582`/`+2584` 一起提交；执行例程对它做 switch。 | `0x108538B0`、`0x19`、`0x31`、`0x5C`、`FF 04/05` |
| `+2582`、`+2584` | `u16` | 当前命令参数（移动状态的 act 与 step 下标）。 | `0x108538B0`、`0x19`、`0x81`、`0x5C` |
| `+2587` | `u8` | `0x05` action 请求的第三个操作数。 | `0x1086A094` |
| `+2588` | `u8*` | lane 位 `0x0001` 的游标；也是 `FF F9/FA/FD` 恢复的游标。 | `0x10860700`、`0x108614F0` |
| `+2592` | `u8` | `FF FE` 读取的 route-move 模式（0/1/2 分别表示 step、递增或重选）。 | `0x108675A0` |
| `+2593`、`+2594` | `u8` | route 选择子：`FF FF` 用 `main[2][+2594]`，`FF FE` 用 `+2593`。 | `0x108675A0` |
| `+2595` | `u8` | `em->cmd_route_act`：`FF FD` 传给 `route_ptr_set` 的 act 下标。 | 字符串 `0x1199DE88` 与 `0x10867EEF` |
| `+2596` | `u8*` | lane 位 `0x0010`（事件 lane `0x10`）的游标。 | `0x10860500` |
| `+2600` | `u8` | 模式字节 `+2680` 的副本；`FF FC` 在调用 `Em_Mode_Chg(em, EM_MODE_ATTACK)` 前存下它。 | `0x10867E98`、`0x10867EA9` |
| `+2601` | `u8` | 触发锁存，由 `0x108693E0` 置位；tick 门 `0x1085C5E0` 只在它置位时运行解释器，然后清掉它。 | `0x1085C5E0` |
| `+2604` | `u8*` | lane 位 `0x0020`（事件 lane `0x20`）的游标。 | `0x10860500` |
| `+2608` | `u8*` | `0x16` 调用脚本表项前保存的后续指令位置，由 `FF 03` 恢复。 | `0x10861CF0`、`0x108675A0` |
| `+2612` | `u8` | `em->cmd_pl_target`：选中的目标下标，`0xFF` = 无（`NOT_FOUND_PL`）。该下标是**域内下标**：它索引哪个数组由已提交的命令 kind `+3231` 决定，而不是由字段名决定。 | kind 13 → `dword_1ED7AD2C + 3824*(slot % 40)`（`0x108538B0` `0x1085417B`、`0x10855B30` `0x10855B3D`、`0x7E` `0x10865818`）；其他 kind → `byte_1DC6B750 + 4176*slot`（`0x10853978`、`0x10855B6E`）；字符串 `0x10867D41`；由 `0x10864A30` 写入，被 `0x7E`/`0x58` 覆盖，被 `0x49`、`0x83`、`FF F7` 清除 |
| `+2615`、`+2616` | `u8`、`u8*` | 选中的地面区域 route 下标和它的保存游标（`0x18`、`FF FB`）。 | `0x108675A0` |
| `+2620`、`+2621` | `u8` | route 等待标志；进入移动段时清 `+2621`，`+2620` 决定 `FF FB` 是否结束区域移动。 | `0x108675A0` |
| `+2622` | `u8` | 重启请求。每条终止路径都会设置它；`0x10860430` 通过把游标倒回 `main[0]` 来消费它。 | `0x10869E77`、`0x1086A0D3`、`0x10860430`、`0x10855470` |
| `+2623` | `u8` | `0x24` 的重复计数器，`0x25` 清零；`24 01` 递减后按有符号字节判断，仍为正时回到 `+2632` 保存的位置。 | `0x108699F3`、`0x108628F0` |
| `+2628` | `u32` | 由重启收尾清除。 | `0x10860430` |
| `+2648` | `u8*` | 地面区域移动开始时保存的游标；由 `FF 04/05/06` 恢复。 | `0x108675A0` |
| `+2652` | `u8*` | handler 返回的当前分派游标；每条重置路径也会更新它。 | `0x1086970A` |
| `+2656` | `u8` | 从当前移动 step 记录取出的动作字节（step 字段 `+18`）。 | `0x108676F0`、`0x10867AA7` |
| `+2657` | `u8` | 移动记录里当前的 act 下标，钳在记录的 act 数内。 | `0x10867690` |
| `+2658` | `u8` | act 里当前的 step 下标，钳在它的 step 数内。 | `0x108676A8` |
| `+2659` | `u8` | 脚本状态：`0` 运行中，`1` 位置/route step 已应用，`2` 停机/已重置。 | `0x10869E6E`、`0x1086A0CA`、`0x1086EAD0` |

### 命令请求、模式与目标选择

| 偏移 | 类型 | 含义 | 证据 |
| ---: | --- | --- | --- |
| `+1252` | `u8` | 逐 actor 的分类字节。目标选择比较 `(candidate[1252] ^ self[1252]) & 1`，低位相同的配对会被拒绝。 | `0x10864A30`、`0x10864D90` |
| `+1826` | `u8` | 已提交的目标下标，`0xFF` = 无；目标门接受时拷进 `+2612`，记录门会再读它。域规则与 `+2612` 相同：kind 13 索引 3824 步长数组，其他 kind 索引 4176 步长（玩家）数组。 | `0x10864A30`；在 `0x108538B0` 提交（case 1 `0x10853972`，case 13 `0x1085416F`）；由 `0x9B`（`0x10866932`）和 `0x7E`（`0x10865691`）解析 |
| `+12` | `u16`/`u8` | 记录自己在所属数组里的下标。`0x7E` 把选中记录的 `+12` 拷进 `+2612`/`+2584`，这就是 kind 13 下标再次指回该记录的原因。 | `0x10865815`；玩家数组的 `+12` 被 `0x101C5FA0` 用作有界下标（`< 8`） |
| `+2680` | `u8` | `Em_Mode_Chg` 消费的 actor 模式；发行版打印为 `EM_MODE_ATTACK` 的值是 `1`。 | 字符串 `0x10867EA1` 与 `0x10867E91` 的 `push 1; call sub_108693E0` |
| `+2681` | `u8` | 命令请求被接受时置 `1`。 | `0x10855470` |
| `+2682` | `u8` | 与 `+2681` 一起存下的请求载荷字节。 | `0x10855470` |
| `+2684` | `u8` | 模式回到 `0` 时与 `+2687` 和仇恨表一起清除的标志字节；各请求 helper 会单独置位/清位。 | `0x108693E0`、`0x10854A60`、`0x10854B90`、`0x10854C70` |
| `+2685` | `i8` | 全局 40 字节步长表 `word_1ED6CEB0` 的下标，`-1` = 无；actor 初始化时该项失效。 | `0x1086EAD0` |
| `+2687` | `u8` | 仇恨下标标志位，每个玩家下标一位。 | `FF F7`、`0x10864A30` |
| `+2688 + 2*i` | `u16` | 每个玩家下标的仇恨值。 | `FF F7`、`0x5B` |
| `+2735`..`+2737` | `u8` | 三类计时请求的已触发标志，分别对应计时值 `+2704`、`+2696`、`+2700` 和优先级 `0x40`、`0x50`、`0x60`；标志为零且计时值为零时置位并产生待处理请求。 | `0x10856290`、`0x10856220`、`0x10856170` |
| `+2739` | `u8` | 命令处理模式标志（`EM_NET_CMD` 路径）。非零时抑制 `0x05` 和 `0x10850CA0` 里的本地动作分派。 | `0x10850CA0`、`0x10867F20` |
| `+2740 + 4*i` | `f32` | 逐下标的候选权重。多个下标合格时，选择保留权重最高的那个。 | `0x10864A30`、`0x10864D90`、`0x10865F30` |
| `+2788 + 4*i` | `u32` | 每个玩家下标的第一张仇恨表；`0x4B` 清除其中一项。 | `0x4B`、`FF F7` |
| `+2824 + 4*i` | `u32` | 每个玩家下标的第二张仇恨表；`>= 50000` 的项直接选中候选，第二轮里 `>= 30000`。 | `0x10864A30`、`0x4C`、`0x4E/0x4F/0x50` |
| `+3188` | `u8` | 当前已授予请求的优先级。`sub_10855BD0` 提供 `0x80/0x70/0x60/0x50/0x40`；`sub_10855470` 只接受 `>=` 已存值的新值。 | `0x10855BD0`、`0x10855470` |
| `+3189`..`+3193` | `u8` | 五个优先级类别的待处理请求标志：`+3189` `0x80`、`+3190` `0x70`、`+3191` `0x60`、`+3192` `0x50`、`+3193` `0x40`。 | `0x10855BD0` |

`0x1E` 在 `0x10869978` 清零 `+2681..+2682`，在 `0x1086997F`、
`0x10869985` 清零 `+3188..+3193`，在 `0x1086998C`、`0x10869993`
清零 `+2735..+2737`。这些字段用于行为请求的选择与重复触发控制；
没有请求队列顺序，也不累计同类请求。清除不修改目标字段或计时值。

### Route、位置与其余状态

| 偏移 | 类型 | 含义 | 证据 |
| ---: | --- | --- | --- |
| `+2840`、`+2841`、`+2842`、`+2843` | `u8` | `FF FE`/`FF FF` 使用的 route-move step 值、上限、进度计数和范围。 | `0x108675A0` |
| `+2844`、`+2845` | `u8` | `FF FB` 测试的地面区域移动进度计数和上限。 | `0x108675A0` |
| `+2846` | `u16` | route step 替换 `+2040` 前保存的 motion id。 | `0x10860430`、`0x108675A0` |
| `+2852`、`+2856`、`+2860` | `f32` | 距离门使用的参考点（x/y/z）：`0x22`/`0x36` 系列 handler 从它减去 `+172`/`+180`，再拿水平长度与缩放后的操作数比较。 | `0x108625B0`、`0x10862700` |
| `+2900` | `void*` | 地面区域对象；`[+24]` 是传给 `0x10857720` 的 act/motion 列表。 | `0x1086766F` |
| `+2912` | `u16` | `0x61` 写入的事件表值，它把 `off_118C7308[species << 4]` 传给 `0x1085C170`。 | `0x10869DD7`、`0x10869DE8` |
| `+2930` | `u8` | `0x75` 拿来与操作数列表比较的值。 | `0x10866CB6` |
| `+3182` | `u8` | 解释器一次调用达到 999 次分派预算时置 `2`。 | `0x108696D0` |
| `+3185` | `u8` | `0x57` 标记选择读取的字节。 | `0x10865988` |
| `+3187` | `u8` | 存储位置有效时非零；门控 `0x18` 和 `FF 04/05/06` 里的"硬"route/step 放置。 | `0x108675A0` |
| `+3200` | `u8` | actor 初始化清除、`0x59` 门读取的标志。 | `0x1086EDE9`、`0x10865A92` |
| `+3206` | `u16` | actor 尺寸系数；初始化时乘以 `f32` 常量 `dword_119B5EF0`（`0.01`），并缩放 `+184/+188/+192`。 | `0x1086EAD0` |
| `+3208` | `u16` | `maji_next_stage_no`，由 `0x10869917` 的调试字符串命名；`0x19` 把它拷进命令参数。 | `0x10869917`、`0x10860430` |
| `+3212` | `i16` | `0x71` 比较的有符号阈值。 | `0x108645B0` |
| `+3228` | `u16` | 由 `0x1085C5E0` 递减的 tick 延迟；只有它为零时解释器才运行。`0x48` 写入操作数，`0x48` 带操作数 `0` 时清除它。 | `0x1085C5E0`、`0x10869C32` |
| `+3231`、`+3232`、`+3234` | `u8`、`u16`、`u16` | 当前移动状态保存的命令 kind、act 下标与 step 下标。 | `0x108538B0`、`FF 04/05`、`0x5C` |
| `+3250` | `u8` | 由重启收尾清除，也在 `FF FE` 重选 route step 时清除。 | `0x10860430` |
| `+3268` | `u8` | `0x53` 选择用 bit `0x20` 测试的标志字节。 | `0x10864D90`、`0x1086E490` |
| `+3288` | `u16` | 活动 lane 掩码。位：`0x01` 基 lane（`+2588`）、`0x02` `FF FA` lane、`0x04` `FF F9` lane、`0x08` `FF F8` lane、`0x10` lane `+2596`、`0x20` lane `+2604`、`0x40`/`0x80` 两条互斥事件 lane。 | `0x10860700`、`0x108614F0` |
| `+3452` | `void*` | 控制器对象；`+1042 & 2` 选择它的 vtable 槽 `+2608`，而不是逐物种更新。 | `0x1086E8A0` |

### `+2544` 以下的实体字段

| 偏移 | 类型 | 含义 | 证据 |
| ---: | --- | --- | --- |
| `+3` | `u8` | 物种 id。选择 `off_11A4BD68[map][species]`（`0x10860360`，`map` = `dword_1E7FFF3C+0x34`）、逐物种更新表 `funcs_1086E91E`（`0x1086E912`）、逐物种位置表 `dword_118C5784[10*species]` 和移动数学的 switch。值 `>= 0xB1` 钳到 `0`。 | `0x10860360`、`0x10868210`、`0x1086E8C9`、`0x10AA5D19` |
| `+6` | `u8` | 移动数学里物种 `115` 分支查询的实体字节；领域角色未证明。 | `0x1086795E` |
| `+20`、`+21` | `u8` | 实体类别/模式对；`0x1086E8A0` 把 `+21 == 6 && +20 == 0xFF` 当作事件/岩石情形，`+21` 为 `5` 或 `6` 时解释器立即返回。 | `0x108696E8`、`0x1086E8A0` |
| `+164` | `u16` | 朝向/角度；route step 前从移动记录拷入，`0x99` 以 `operand << 8` 写入。 | `0x1086778F`、`0x10867B83`、`0x10869F30` |
| `+172`、`+176`、`+180` | `f32` | 世界坐标 x/y/z。距离门（`0x22`、`0x36`、`0x42`、`0x69`、`FF 04/05/06`）读取，route step 或 `0x90` 写入。 | `0x108625B0`、`0x10868210` |
| `+1784`、`+1788`、`+1792` | `f32` | route step 开始或 `0x90` 放置 actor 时留下的上一位置快照。 | `0x10867777`、`0x10868210` |
| `+1040` | `u8` | `0x1F` 与 `1` 比较、`0x51` 与 `4` 比较的状态。 | `0x10861050`、`0x108635A0` |
| `+1042` | `u16` | 实体状态字。逐帧更新测试 `0x0800`（跳过 AI）与 `0x0002`（控制器 vtable 路径）；`0x85` 置 bit `0x40` 并调用 `0x113AAAC0`，`0x86` 清除它。 | `0x10869FFE`、`0x1086A011`、`0x1086E8A0` |
| `+1096` | `u16` | 自由运行计数器，用作脚本 RNG：`0x7B`/`0x84` 递增，`0x80` 掩到 5 位，`FF FE` 用作取模下标。 | `0x10869EF3`、`0x108675A0` |
| `+1100` | `void*` | 解析后的目标记录指针，`0` = 无：玩家域是 `byte_1DC6B750 + 4176*slot`，命令 kind 13 是 `dword_1ED7AD2C + 3824*slot`。`0x10855B30` 和 `0x85` 接触例程读它来取目标位置。 | `0x108538B0`（`0x10853978`、`0x1085417B`）、`0x10855B30`、`0x113AAAC0` |
| `+1140` | `void*` | `0x79` 可以调用其 `+12` 函数指针的对象。 | `0x10864750` |
| `+1390` | `u8` | 为 `1` 时会阻止物种 `80/81/90` 的 `Em_Mode_Chg` 的字节。 | `0x108693E0` |
| `+1826` | `u8` | 与上文同一个字段；`0x9B` 把它读作下标并通过 4176 步长数组解析。早先的 `void*` 读法来自 `0x7F`（`0x10866770`），它的第一个参数是字节码游标 `cursor+1`，不是 `record + 1826`。 | `0x10866770` 分派 `0x10869FE0`（`lea eax, [edi+1]`）、`0x108668E0` |
| `+2008`、`+1800` | `void*` | route step 放置实体时传给 `0x108CE270` 的缓冲区。 | `0x108675A0` |
| `+2040` | `u16` | 当前 act/motion id；act 列表查找 `0x10857720` 的键，step 后作为新 motion 写回。 | `0x10855BD0` |
| `+2068` | `f32` | `0x78` 与 `+164` 一起使用的角度来源。 | `0x10864750` |
| `+2088` | `u8` | `Em_Mode_Chg` 与 `2` 比较的字节；`0x0A` 写入它。 | `0x108693E0` |
| `+2227` | `u8` | 模式回到 `0` 时与仇恨表一起清除的逐下标位域。 | `0x108693E0` |

## 同一批证据命名的 helper 例程

| 地址 | 名字 | 证据 |
| --- | --- | --- |
| `0x10860340` | `em_cmd_top(em)`：返回 `main[0][+2576]` 并设 `+2580 = 0`。调用方想要栈顶条目时会先清零 `+2576`。 | 字符串 `0x10867F20` 与 `0x10867EB9` |
| `0x10860430` | 重启收尾：`+2622` 置位时清 `+3288`、`+3228`、`+2601`、`+2622`、`+3250`、`+2576`、`+2580`，从 `main[0]` 重装 `+2652`/`+2548`，把 route 进度字段设为 `-1` 并清 `+2628`。 | `0x10860430` |
| `0x108604C0` | `route_ptr_set(em, em->cmd_route_act)`：返回 `main[2][act]`。 | 字符串 `0x1199DE88`、`0x108604C0` |
| `0x10855470` | 命令请求接受：校验全局 id，保留最高优先级并设置 `+3188`、`+2681`、`+2682`、`+2622`。 | `0x10855470` |
| `0x10855BD0` | 命令请求阶梯：从 `0x80` 到 `0x40` 走 `+3189..+3193`，清掉胜出标志。 | `0x10855BD0` |
| `0x10864A30` | 目标选择：尊重 `+1826`，否则按 `+2740` 给仇恨/标志合格的项排名。 | `0x10864A30` |
| `0x108538B0` | 命令提交例程：对 `+2581` 做 switch；case 1 把目标下标解析进 `byte_1DC6B750`（`+1100`、`+1826`），case 13 解析进 `dword_1ED7AD2C`；还把 `+2581`/`+2582`/`+2584` 镜像到 `+3231`/`+3232`/`+3234`。 | `0x108538B0` |
| `0x10855B30` | 目标位置 helper：`+3231 == 13` 时从 `dword_1ED7AD2C + 3824*slot` 读位置，否则从 `byte_1DC6B750 + 4176*slot`。 | `0x10855B30` |
| `0x10868210` | `0x90` handler：把 `dword_118C5784[10*species][index]` 拷进 `+172/+176/+180` 并快照 `+1784..`。 | `0x10868210` |
| `0x108693E0` | `Em_Mode_Chg(em, mode)`：写 `+2680`，清 lane 事件位，模式为 `0` 时清 `+2684`、`+2687`、两张仇恨表和 `+2227`。 | 字符串 `0x10867EA1` |
| `0x1086EAD0` | actor 初始化/重置：清 `+3198` 和 `+2964` 块，强制脚本状态 `2` 与 `+2622 = 1`，重装脚本表，重置位置并写入逐物种尺寸系数。 | `0x1086EAD0` |

## 数据表

| 地址 | 含义 | 证据 |
| --- | --- | --- |
| `0x11A4BF74` | actor 类型名指针数组：`[0]` `EM_EXC`，`[1..14]` `KIND_NOT`、`KIND_PLY`、`KIND_GND`、`KIND_ARA`、`KIND_ITM`、`KIND_DEM`、`KIND_MYP`、`KIND_SML`、`KIND_YOB`、`KIND_ART`、`KIND_APL`、`KIND_LND`、`KIND_DMG`、`KIND_EMY`。 | `0x11A4BF74` 数据 |
| `0x11A4BFB0` | `10000`，紧跟类型表的 act-id 基数；`0x11A4BFB4`/`0x11A4BFB8` 存 `20000`/`30000`。act id `>= 10000` 按 `id - 10000` 处理。 | `0x11A4BFB0` 数据、`0x10855BD0` |
| `0x11869168` | 逐 id 记录表，步长 `40`；字段 `+0` 是 `u16` 类型，值 `1` 和 `3` 经 `sub_10B4C410` 归一化，字段 `+4` 是指针（即 `word_11869168`/`off_1186916C` 对）。 | `0x1085C5E0`、`0x10855BD0` |
| `0x1188EAB0` | 五个 `u16` id `{361, 363, 365, 367, 369}`，与记录的 `+2040` 比较；命中时比较值折叠为 `1`。 | `0x1188EAB0` 数据、`0x108656DB`、`0x1086A530` |
| `0x11A4EC90` | 逐 actor-id 指针数组（`0x11A4EC90[id][stage]` → `u16` 列表，`0xFFFF` 终止）。玩家域目标门 `0x108695F0` 使用它，它把列表项和候选的 `+2040` 都经 `sub_10B4C410`/`sub_10B4C4E0` 归一化；列表项 `>= 10000` 按 `id - 10000` 比较。读到的列表都是小 id（`11..178`），所以这个门是 id 白名单，不是区间。 | `0x108695F0`、`0x11A4EC90` 数据、`0x11A4C310` |
| `0x118C5784` | 逐物种指针数组，每个物种 `10` 个 dword，指向 `0x90` 操作数选中的 12 字节位置记录。 | `0x10868210` |
| `0x11C6A7E8` | 全局 AI 分支选择值（`i32`），AI 表初始化 `0x10860360` 写为 `0`。`0x94` 与有序 u8 case 精确匹配；遇到更大的 case 跳至默认分支或结束，不执行该 case。非零值的来源与业务命名未确认。 | `0x108603E9`、`0x108682B0`、下节原生脚本 |
| `0x119B5EF0` | actor 初始化缩放尺寸字段时使用的 `f32` 常量 `0.01`。 | `0x1086EAD0` |
| `0x1199DE88`、`0x1199DEC0`、`0x1199DE00`、`0x1199DE5C`、`0x1199DF14` | 给 `FF` 控制命令和主表跳转命名的调试字符串。 | 字符串 |

## 0x94 全局 AI 分支选择值

`0x108682B0` 读取共享全局 `0x11C6A7E8`，不读取怪物的 `+2680` 模式字节。
规范布局为 `94 00 <count>`、若干 `94 01 <u8 value>` 分支、可选
`94 02` 默认分支和 `94 03` 结束标记。比较使用有符号 i32 全局值与零扩展的
u8 case；相等进入正文，全局值小于 case 时停止查找并扫描至默认分支或结束。

已核对的 15 个静态脚本都含两个 case：`0` 进入通常的条件／子脚本选择，
`1` 只执行 `82 03 04`，即调用当前根表的 `root[18][4]`，没有默认分支。
该槽位的含义取决于根表，不能把全局值 `1` 命名为某个通用招式：

| 根表示例 | case 1 的目标 | 目标脚本 |
| --- | --- | --- |
| 火龙 `0x11A3EDD0`、`0x11A3E220` | `0x11847608` | `05 03 02 00 FF 02`：`self.action(3:2, 0)` 后返回 |
| 岩龙 `0x11A13C68` | `0x11779574` | `13 05 03 04 00 FF 02`：绑定当前目标后执行 `self.action(3:4, 0)` |
| 铠龙系 `0x11A17268`、`0x11A17418`、`0x11A176F0` | `0x117A6004`、`0x117A6810`、`0x117A782C` | 同样为 `13 05 03 04 00 FF 02` |
| 刻龙 `0x11A3EE30`、`0x11A3E2A8` | `0x11833B74` | 仅 `FF 02`，不执行动作 |

对应的 0x94 脚本位于 `0x1177932C`、`0x117A5648`、`0x117A65C0`、
`0x117A6CA0`、`0x11848460`、`0x118492D8`、`0x1184AD20`、`0x1184C7F0`、
`0x1184E278`、`0x1184FCA8`、`0x11850EC8`、`0x11852118`、`0x118539A8`、
`0x11854D88`、`0x11978760`。表中物种关系来自地图 `0..97`、静态物种槽
`0..131` 的根表目录；不代表同一物种在所有地图使用同一个根表。

### 设置来源与地址隔离

当前 DLL 的直接引用为初始化清零 `0x108603E9` 和读取 `0x10868303`。
初始化函数在 `0x10860369` 执行 `xor ebx, ebx`，再以 EBX 写入该全局；
它由 actor 初始化路径 `0x1086EAD0` 的 `0x1086EB51` 调用，而非仅在程序启动时执行。
变量位于 PE 节的零填充虚拟尾部，没有磁盘中的非零配置初值。其内部字节
`+1..+3` 也没有单独的直接引用；全局地址没有作为数据指针出现在文件中。

相邻存储的已确认边界：

- 前方 `0x11C6A7C8..0x11C6A7E7` 属于 `0x10837DC0` 的 UI 矩形；
  索引被限制在三个 8 字节条目内，额外的矩形也只占 `+E0..+E7`。
  `0x108F29B0` 按调用方给定的条目数读取矩形，不向其后方写入。
- 更前方的 `0x11C6A60C` 是六个 dword 的位集。`0x10836340` 的索引写入
  检查下标 `<= 5`，不会延伸至该选择值；`0x11C6A624` 是另一个计数字段。
- 后方 `0x11C6A7F0` 是三个 16 字节的事件回调记录，初始化函数为
  `0x1087ECB0`、`0x1087F110`，调度函数为 `0x1087F710`。
  其描述目录 `0x11A46130..0x11A465B0` 的 48 个条目包含 14 个不同回调。
  这些回调及其记录指针转发路径没有向记录头之前的负偏移写入；
  `0x1087FFB0`、`0x10880180` 等共享处理函数也不是该选择值的设置方。
  邻接的 `0x11C6A7EC` 事件状态不能当作 `0x11C6A7E8` 的别名。

同一组样本中的 `mhfo.dll` 的对应处理函数为 `0x1084D810`，读取全局 `0x11C12C30`。
其 201 字节函数体与 HD 版仅全局地址操作数不同；地址字面量仍只有
`0x10845949` 的初始化写入和 `0x1084D863` 的读取，对应 RVA 字面量没有命中。
两版导出表均不导出该变量。同组游戏文件中的 `mhf.exe`、`mhfi.exe`、`mhl.dll`、
`mhf-zz.exe`、`hello.exe`、`gdiplus.dll`、`ogg.dll`、`vorbis.dll` 未包含
这两个变量的 32 位 VA 或 RVA 字面量。这些文件检查不能排除运行时地址计算、
未检查模块、外部工具或注入代码的写入。

非零值写入路径尚未确认。因此可确认其在上述脚本中选择“常规 AI／预设分支”，
但是否为测试或调试开关仍未证实，也不能由直接引用数断言它永远为零。
DSL 以约定名称 `match context.debug_mode` 表达完整有序的数值分支，
不提供设置操作，也不把调试用途视为已证实的原生语义。非规范布局保留 `native(...)`。

## 昼夜分支（0x77）

`0x1ED6BCA0` 的 `0x08` 为昼标志，`0x10` 为夜标志。
`0x108665F0` 的 selector 0 在 `(flags & 8) != 0 || (flags & 0x10) == 0`
时进入正文，仅夜标志设置而昼标志未设置时跳至 else 或结束。
selector 1 为 else，selector 2 为结束。两位均清除或均设置时都进入正文，
因此完整语义不能缩减成单独读取昼标志。

写入与独立消费者证据：

- `0x106EF910` 根据全局时间值 `0x1EE24964` 与 `0xA8C0`（43200）的比较，
  清除这两位后分别设置 `0x08`（小于）或 `0x10`（大于等于），并保存昼夜索引到 `0x1ED52840`。
- `0x107160F0` 检测该时间侧相对已保存索引发生变化时，按同一个比较结果从
  `0x11879958` 的两个 4 字节记录选择音频 ID：索引 0 为 169，索引 1 为 168。
- 音频初始化 `0x114DC510` 设置音乐表基址 `0x115F99C8`；
  `0x114DC310` 以 `16 * ID` 索引文件名。169 对应 `ef_c_n_d.mus`（夜转昼），
  168 对应 `ef_c_d_n.mus`（昼转夜），与昼夜方向相互印证。

该指令读取环境标志，不直接读取时钟，不改变昼夜，也不是倒计时条件。
DSL 使用 `if context.is_daytime`；完整可无损往返的条件结构恢复为 if/else，
非规范布局保留 `native(...)`。

## 仍未命名

这些操作数被已分派 opcode 消费，但还没有解释器之外的消费者证明其角色。它们
继续按地址记录。该清单的机器可读形式是 `opcodes-*.json` 记录里的
`domain_unresolved` 数组；两者必须保持同步。

- `0x1E8001EC`、`0x1E7FFF3C` —— 指向全局状态块的运行时指针；目前只有个别
  字段（`+8`、`+20`、`+128`、`+9210`）有已证明的用途
  （`0x56`、`0x60`、`0x72`、`0x74`、`0x76`、`0x7A`、`0x7C`）；
- `0x11C6A7E8` —— `0x94` 的全局 AI 分支选择值；非零值来源和业务命名未确认；
- `0x1ED52870`、`0x1ED52951` —— 被全局门读取的引擎全局；
  `0x1ED6BCA0` 的昼夜两位已确认，其余位不由 `0x77` 解释；
- `+27`、`+1005`、`+1112`、`+2729`、`+2912`、`+2930`、`+3185`、`+3200`、
  `+3268` —— 只在解释器内部有已证明用途的实体字段；
- `+2696`、`+2700`、`+2704` 与 `+2708`、`+2712`、`+2716` —— 两组三个，
  被 `0x21`、`0x4e`、`0x4f`、`0x50` 和 `0x9a` 当作（值，缩放）对读取。
  配对和运算已证明，角色没有：解释器真正打印过的逐下标仇恨结构是 `+2687`、
  `+2688 + 2*i`、`+2788 + 4*i` 和 `+2824 + 4*i`。`opcodes-*.json` 里的
  "hate accumulator" 标签是这六个偏移的描述性占位名，不是已证明的游戏词汇
  —— 尤其 `+2712` 在 `0x9a` 里是乘操作数而不是给它设界，所以 "limit" 也不
  合适；
- `0x118648F0`（`0x7A`、`0x7C`）、`0x1188EAB0`（`0x5F`、`0x7E`）、`0x11406AF0`
  （`0x45`）、`0x118C7308`（`0x61`）、`0x118E45B0`/`0x119B63A8`（`0x41`）、
  `0x1ED7AD2C`/`0x1EDB768E`（`0x7E`）以及角度常量
  `0x116038A8`/`0x119B5F9C`/`0x119B5FA0`（`0x42`、`0x69`）—— 作为数据被
  索引或作为调用操作数使用，但行含义未证明；
- `0x11869168`/`0x1186916C`（`0x38`、`0x5E`）—— 逐 id 记录表已证明
  （步长 40，`+0` 是类型，`+4` 是指针），但类型值的含义没有证明，所以两条
  记录都保持 `generic`。

将来任何命名都必须来自原生消费者，而不是表的形状。

## 已解决 / 待定项

- `+1826` 已定：它是解释器记录上的 `u8` 目标下标（`0xFF` = 无）。与之竞争
  的 `void*` 读法来自 `0x7F` 的 handler `0x10866770`，它的第一个参数是字节码
  游标（`cursor+1`），不是记录的字段——分派处以 `lea eax, [edi+1]` 装入它。
- `+2612`/`+1826` 不是无条件指向玩家下标：它们按已提交的命令 kind 解析到两个
  不同数组（`1`/`11` → `byte_1DC6B750`，`13` → `dword_1ED7AD2C`）。玩家域
  路径（`0x10864A30` + 仇恨表 + `FF F7` 的 `NOT_FOUND_PL`）已证明；kind 13
  路径挑选同一 `+2040` 家族的*另一条*记录（`0x7E`），或拷关联 actor 自己的
  目标下标（`0x58`）。**未**证明的是通用的怪物对怪物目标选择：kind 13 选择器
  要求候选的 `+2040` 与本记录相等，所以它是配对/关联 actor 情形，不是任意
  目标。
- "关联 actor 指针"这个描述附着在多个不同字段上（`0x27` 的 `+27`、`0x47` 的
  `+2729`、`0x3B`/`0x3C`/`0x44`/`0x58` 的 `+3172`、`0x57` 的 `+3185`、
  `0x59` 的 `+3200`、`0x83` 的 `+1112`）。只有 `+3172` 有
  `domain_notes` 级证据（`sub_108578A0` 扫描 `dword_1ED7AD2C`）；其余保持
  `generic`，不得并入同一个名字。
